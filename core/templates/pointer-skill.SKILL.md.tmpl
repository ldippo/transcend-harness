---
name: {id}
description: {what} Use: {when}
user-invocable: true
---

# {id} (pointer)

Wired into this project's harness by transcend-harness. Run `/{id}` — {what}

> This is a thin pointer. Replace it with the real `{id}` skill/plugin when
> available; the harness references it from {pillar_rule_ref}.
