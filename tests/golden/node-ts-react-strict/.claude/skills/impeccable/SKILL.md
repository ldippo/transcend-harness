---
name: impeccable
description: Frontend design critique & UX auditing (visual hierarchy, spacing, states, a11y heuristics). Use: UI work in a component-based frontend.
user-invocable: true
---

# impeccable (pointer)

Wired into this project's harness by transcend-harness. Run `/impeccable` — Frontend design critique & UX auditing (visual hierarchy, spacing, states, a11y heuristics).

> This is a thin pointer. Replace it with the real `impeccable` skill/plugin when
> available; the harness references it from .claude/rules/quality-gates.md.
