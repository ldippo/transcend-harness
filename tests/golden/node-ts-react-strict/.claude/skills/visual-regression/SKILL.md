---
name: visual-regression
description: Screenshot capture + perceptual diff to catch unintended UI changes. Use: Visual UI that shouldn't change unintentionally.
user-invocable: true
---

# visual-regression (pointer)

Wired into this project's harness by transcend-harness. Run `/visual-regression` — Screenshot capture + perceptual diff to catch unintended UI changes.

> This is a thin pointer. Replace it with the real `visual-regression` skill/plugin when
> available; the harness references it from .claude/rules/quality-gates.md.
