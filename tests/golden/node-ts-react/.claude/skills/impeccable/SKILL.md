---
name: impeccable
description: Frontend design critique & UX auditing — visual hierarchy, spacing/rhythm, component state coverage (loading/empty/error), responsive behavior, and accessibility heuristics. Use before merging changes that touch UI components.
user-invocable: true
---

# impeccable (pointer)

Wired into this project's harness by transcend-harness. Run `/impeccable` against the
current diff or a target component before merging UI changes.

> This is a thin pointer. Replace it with the real `impeccable` skill/plugin when
> available; the harness references it from `.claude/rules/quality-gates.md`.
